<%= content_tag_for(:li, medium, :class => 'span3') do %>
	<%= link_to medium.attachment_url(:modal).to_s, :class => 'thumbnail colorbox' do %>
		<%= image_tag (medium.attachment_url(:thumb).to_s) %>
	<% end %>
<% end %>
